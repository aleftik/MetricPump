package com.appdynamics.metricpump;

import com.appdynamics.metricpump.appdynamics.model.AppDynamicsMetric;
import com.google.gson.JsonArray;

public class MetricWriterUtilsV2Test {

    public void testAddTagsForHaProxyMetrics() throws Exception {
        JsonArray tags = new JsonArray();
        AppDynamicsMetric metric = new AppDynamicsMetric();
    }
}
